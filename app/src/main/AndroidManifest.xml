<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.yoshievinsmoke.ayomangan">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".app.AppController"
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/ic_launcher"
        android:label="ayomangan"
        android:largeHeap="true"
        android:roundIcon="@drawable/splash"
        android:theme="@style/AppTheme">

        <!-- Splash screen -->
        <activity
            android:name=".Splashscreen"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main activity -->
        <activity
            android:name=".Login"
            android:label="@string/app_name" />
        <activity
            android:name=".Register"
            android:label="@string/app_name" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".CustomerActivity"
            android:label="" />
        <activity android:name=".datail_makanan" />
        <activity
            android:name=".detail_pesanan"
            android:label="@string/title_activity_detail_pesanan"
            android:theme="@style/AppTheme" />
        <activity android:name=".detailpesanan" />
        <activity
            android:name=".MainActivityKurir"/>
        <activity android:name=".detailpesanan_seller"></activity>
    </application>

</manifest>